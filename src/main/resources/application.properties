# Use port 8080 locally and allow Railway or another platform to provide PORT.
server.port=${PORT:8080}
# Listen on every network interface so the cloud platform can route traffic here.
server.address=0.0.0.0
server.shutdown=graceful
